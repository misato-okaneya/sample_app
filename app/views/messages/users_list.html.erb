<% provide(:title, 'Select a user to send a new message') %>
<h1> Send a message to... </h1>

<%= will_paginate @users %>

<ul class ="users">
  <% @users.each do |user| %>
    <%= render 'user', user: user %>
  <% end %>
</ul> 

<%= will_paginate @users %>